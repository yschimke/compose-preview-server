// Generated by generate.py from model.json. This exact file is compiled and tested.
package poc

import androidx.compose.foundation.Canvas
import androidx.compose.foundation.background
import androidx.compose.foundation.clickable
import androidx.compose.foundation.layout.*
import androidx.compose.material3.Text
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

@Composable
fun GeneratedInterface(onHostAction: (String) -> Unit = {}) {
  var mode by remember { mutableStateOf(0) }
  Column(Modifier.size(320.dp, 260.dp).background(Color.White).padding(@@PADDING@@.dp)) {
    Text(@@TITLE@@, fontSize = 24.sp, color = Color(0xFF17212B))
    Spacer(Modifier.height(16.dp))
    Box(
      Modifier.size(288.dp, 72.dp).clickable {
        mode = 1 - mode
        onHostAction("lighting.changed")
      }
    ) {
      when (mode) {
        0 -> StatePanel("Off", Color(0xFF455A64))
        1 -> StatePanel("On", Color(0xFF006C4C))
      }
    }
    Spacer(Modifier.height(12.dp))
    Canvas(Modifier.size(288.dp, 28.dp)) {
      repeat(@@COUNT@@) { i ->
        drawCircle(Color(0xFF006C4C), 8.dp.toPx(), Offset((12 + i * 24).dp.toPx(), 12.dp.toPx()))
      }
    }
    Spacer(Modifier.height(8.dp))
    Row {
      Badge(@@LABEL0@@)
      Badge(@@LABEL1@@)
    }
  }
}

@Composable
private fun StatePanel(label: String, color: Color) {
  Box(Modifier.fillMaxSize().background(color).padding(16.dp)) {
    Text(label, fontSize = 24.sp, color = Color.White)
  }
}

@Composable
private fun Badge(label: String) {
  Box(Modifier.size(138.dp, 36.dp).background(Color(0xFFE0E9E4)).padding(8.dp)) {
    Text(label, fontSize = 16.sp, color = Color(0xFF17212B))
  }
}
